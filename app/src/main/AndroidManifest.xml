<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ice.repositorioejercicios">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Clases.UserVipApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design">
        <activity android:name=".Dagger2.Dagger2Activity"></activity>
        <activity android:name=".Room.roomActivity" />
        <activity android:name=".Result.ResultActivity" />
        <activity android:name=".SharedPreferences.SharedPreferencesActivity" />
        <activity android:name=".WebView.WebViewActivity" />
        <activity android:name=".Picker.PickerActivity" />
        <activity android:name=".ViewPage.ViewPageActivity" />
        <activity android:name=".ReciclerView.ReciclerViewActivity" />
        <activity android:name=".KotlinBasico.KotlinBasicoActivity" />
        <activity android:name=".AndroidMedio.AndroidMedioActivity" />
        <activity android:name=".FireBase.FireBaseActivity" />
        <activity android:name=".FireBase.HomeFBActivity" />
        <activity android:name=".Inicio.InicioActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>